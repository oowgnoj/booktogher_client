import React, { ReactElement } from "react";
// import { author } from "../shared/InitialStates";
// import user from "../../Redux/modules/user";

// const withIsAuthor = (WrappedComponent: React.FC) => {
//     const newProps = {
//         display: false
//     }

//     author.id === user.id {newProps.display = true}
//     return
// }

// ----
// withLogin (component) { // class 로만 해야하는지

//     return class enhanced {

//         boolean (props.authorid === props.userid)

//         render () {
//             return (<component display=boolean, ... props />)
//         }

//     }

// ----
// 1) routerButton
// const LoginRouterButton = withLogin(routerButton)
// const LogoutRouterButton = withLogout(routerButton)

// export LogoutRouterButton;
// export LoginRouterButton;
// export routerButton;

// 2)
// export withLogin(routerButton)

// ----
// (index)

// <LoginRouterButton author.id=author, userid=user param= page =/>

// <routerButton author. ~~~ />

// }
